#include <stdio.h>
#include <stdlib.h>
#define ll unsigned long long int

void swap(ll *xp, ll *yp)
{
    ll temp = *xp;
    *xp = *yp;
    *yp = temp;
}

void bubbleSort(ll arr[], ll e[], ll n)
{
   ll i, j;
   for (i = 0; i < n-1; i++)
       for (j = 0; j < n-i-1; j++)
           if (arr[j] > arr[j+1])
           {
               swap(&arr[j], &arr[j+1]);
               swap(&e[j], &e[j+1]);
           }
}

ll main()
{
    ll t, t1, n, i;
    scanf("%llu", &t);
    t1 = t;

    while(t--)
    {
        ll flag = 0;
        scanf("%llu", &n);
        ll s[n], e[n], s1[n];
        char result[n];

        for(i=0; i<n; i++)
        {
            scanf("%llu %llu", &s[i], &e[i]);
            s1[i] = s[i];
        }

        bubbleSort(s, e, n);

        ll max_c=0,max_j=0,min_c=0,min_j=0,c=0,j=0;

        for(i=0; i<n; i++)
        {
            if(s[i]>=max_c)
                c=0;
            if(s[i]>=max_j)
                j=0;
            if(c==0)
            {
                c=1;
                min_c=s[i];
                max_c=e[i];
                result[i]='C';
            }
            else if(j==0)
            {
                j=1;
                min_j=s[i];
                max_j=e[i];
                result[i]='J';
            }
            else
            {
                flag=1;
            }
        }

        printf("Case #%llu: ", (t1-t));
        if(flag==1)
        {
            printf("IMPOSSIBLE\n");
        }
        else
        {
            for(i=0; i<n; i++)
            {
                ll fix = 0;
                if(s1[fix] == s[i])
                {
                    swap(&result[fix], &result[i]);
                    fix++;
                }
            }
            for(i=0; i<n; i++)
                printf("%c", result[i]);
            printf("\n");
        }
    }
}
